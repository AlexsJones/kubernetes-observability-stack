---
bases:
  - ../common/environments.yaml
---
repositories:
- name: helm-remote
  url: https://storage.googleapis.com/solo-public-helm

releases:
- name: gloo
  namespace: gloo-system
  chart: helm-remote/gloo
  labels:
    chart: "gloo"
    repo: "stable"
    component: "ingress"
    namespace: "default"
    vendor: "kubernetes"
    default: "true"
  values:
    - ingress:
        enabled: true
